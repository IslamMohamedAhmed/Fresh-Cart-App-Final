Fresh Cart — Backend API for E-commerce (مختصر للنشر على فيسبوك)

مرحبًا! أنهيت العمل على الـ backend لمشروع Fresh Cart. هذه النبذة السريعة تشرح اللي يهم المستخدم/المطور:

🔹 ماذا يفعل المشروع؟
- API كامل لإدارة المنتجات، الأقسام، الماركات، المستخدمين، السلة، الطلبات، المراجعات، قائمة الرغبات، الكوبونات والعناوين.

🔹 نقاط مهمة للمطورين:
- Endpoints تحت `/api/v1`.
- يدعم: البحث بـ `keyword`، اختيار الحقول `fields`، الفرز `sort`، والفلترة الرقمية `field[gt]`.
- رفع صور لمنتجات/ماركات/أقسام عبر `multipart/form-data`.
- نقاط محمية تحتاج توكن: إضافة/تعديل/حذف بعض الموارد.

🔹 تشغيل محلي:
- انسخ المشروع وشغّل `npm install` ثم `npm start` بعد وضع متغيرات البيئة (`MONGO_URI`, `JWT_SECRET`, `STRIPE_SECRET_KEY`, إلخ).

🔹 ملاحظة أمان:
- لا تضع بيانات الاتصال (مثل كلمات السر) داخل الملفات — استخدم متغيرات بيئة.

لو حابب شرح تفصيلي لكل endpoint أو أمثلة طلب/استجابة أرفعها هنا بإذن الله. تابعوني لو عايزوا نسخة README للـ GitHub.

#FreshCart #Ecommerce #API #NodeJS #Express #Mongoose